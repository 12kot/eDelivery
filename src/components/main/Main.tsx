import React, {ReactElement} from "react";
import styles from "./Main.module.css";

const Main = (): ReactElement => { 
    return (<div className={styles.container}><h1>LOL</h1></div>);
}

export default Main;